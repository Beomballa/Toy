# AGENTS.md

## 작업 방식

Toy 프로젝트에서는 기능 단위로 이어서 처리합니다.

가능하면 분석만 하지 않고 아래 순서까지 진행합니다.

1. 원인 분석
2. 구현
3. 테스트 보강
4. 정리 및 보고

작업 범위를 벗어난 리팩토링보다 기능 완성을 우선합니다.

동일 기능의 기존 구현이 존재하면 새로운 패턴을 만들기보다 기존 패턴을 우선 사용합니다.

가능한 한 최소 수정 원칙을 유지합니다.

---

## Git 원칙

Toy 프로젝트에서는 기능 또는 화면 단위 작업 완료 시 자동 커밋을 수행합니다.

위 자동 커밋 정책은 `Toy` 프로젝트에서만 적용합니다.

`Toy` 프로젝트에서는 기능 또는 화면 단위 완료 시 커밋 진행 여부를 사용자에게 별도로 묻지 않고 자동으로 처리합니다.

기본 작업 종료 범위는 로컬 커밋까지이며 push 는 사용자 요청이 있는 경우에만 수행합니다.

서로 독립적인 기능과 화면 변경은 가능한 한 별도 커밋으로 분리하고, 같은 동작을 완성하는 HTML, CSS, JavaScript와 테스트는 하나의 작업 단위로 커밋합니다.

사용자가 `오늘 한도내에서 작업 처리`라고 요청한 경우, 해당 날짜 작업 스레드 안에서 사용할 수 있는 토큰 한도 내로 기존 기능 수정과 신규 기능 개발을 연속 수행합니다.

위 요청이 있는 경우 다음 작업 수행 여부를 별도로 묻지 않고 기능 또는 화면 단위로 구현, 테스트, 커밋까지 이어서 처리합니다.

커밋은 가능한 한 작업 단위별로 분리합니다.

커밋 메시지는 반드시 아래 형식을 사용합니다.

YYYY.MM.DD <작업 내용>

예시:

2026.06.04 운영 작업 워크로드 export 추가

커밋 전 변경 범위를 확인하고 불필요한 파일이 포함되지 않았는지 검토합니다.

기능 또는 화면 단위 개발 완료 시 테스트 코드 작성 또는 기존 테스트 보강 후 검증이 끝난 경우에만 커밋을 진행합니다.

테스트가 환경 이슈로 실행되지 못한 경우 그 사유를 명시하고 보고 후 다음 작업으로 진행합니다.

---

## 코드 원칙

JPA, QueryDSL은 단순 동작 구현보다 조회 구조, 확장성, 책임 분리를 고려하여 작성합니다.

JPA, QueryDSL 사용 시 단순 CRUD 구현보다 동적 조건 분리, DTO Projection 안정성, 조회 전용 구조 분리, N+1 방지, 페이징/집계 확장성을 함께 고려합니다.

import로 대체 가능한 FQCN(Fully Qualified Class Name)은 본문에서 직접 사용하지 않습니다.

헷갈릴 수 있는 조회 의도나 분기에는 짧고 명확한 주석만 허용합니다.

사용자 요청 없이 아래 작업은 수행하지 않습니다.

* 대규모 패키지 구조 변경
* 공통화 목적 리팩토링
* 아키텍처 변경
* 신규 라이브러리 추가
* build.gradle 의존성 추가

기존 프로젝트 스타일을 우선 유지합니다.

---

## Spring / Backend 규칙

기존 API 계약을 변경하지 않습니다.

다음 항목은 사용자 요청 없이는 변경하지 않습니다.

* URL
* Request Field
* Response Field
* DTO Field Name
* Entity Column Mapping
* DB Column Name

새로운 API 추가 전 기존 Mapping 중복 여부를 확인합니다.

Controller, Service, Repository 계층 구조는 기존 패턴을 우선 따릅니다.

@Transactional 은 명확한 이유 없이 추가하지 않습니다.

조회 구현 시 N+1 가능성을 검토합니다.

---

## QueryDSL 규칙

DTO Projection 시 생성자 순서와 타입을 반드시 검증합니다.

가능하면 Tuple 반환보다 DTO 반환을 우선합니다.

Projection 변경 시 DTO 생성자와 QueryDSL 생성자 순서가 일치하는지 확인합니다.

기존 Repository 구현 패턴을 우선 참고합니다.

---

## Frontend 규칙

기존 Vanilla JavaScript 구조를 유지합니다.

사용자 요청 없이 아래 항목은 추가하지 않습니다.

* React
* Vue
* Alpine.js
* 신규 상태관리 라이브러리

기존 Alert, Confirm, Modal 유틸리티를 우선 사용합니다.

기존 Axios 호출 패턴을 유지합니다.

프론트 화면 구조를 크게 변경하기 전에는 지정된 레퍼런스 사이트의 현재 화면을 직접 확인하고 컨테이너 폭, 여백, 글자 크기, 정보 밀도, 탐색 단계와 모바일 변형을 비교합니다.

고객용 홈은 탐색 진입점 역할을 우선하며 한 화면에 분석 지표, 저장 도구와 관리 기능을 모두 펼치지 않습니다. 상세 기능은 독립 화면 또는 명확한 더보기 경로로 분리합니다.

KREAM형 고객 화면을 구현할 때 아래 구조를 기본 기준으로 사용합니다.

* 상단 유틸리티와 주요 메뉴, 카테고리 탭으로 구성된 2단 탐색
* 데스크톱 기준 약 1,200px 콘텐츠 폭과 40px 안팎의 좌우 여백
* 하나의 핵심 메시지와 행동을 강조하는 단일 프로모션 영역
* 카테고리 바로가기 이후 신규, 인기, 빠른배송과 큐레이션 상품 레일 배치
* 본문 12~14px, 주요 탐색 14~18px, 섹션 제목 18~20px 중심의 절제된 타이포그래피
* 상시 노출 행동은 더보기와 핵심 이동 버튼으로 제한하고 정렬, 일괄 처리와 복사 기능은 오버플로 메뉴로 이동

KREAM의 상표, 문구, 이미지와 소스 코드를 직접 복제하지 않습니다. 정보 구조와 사용 흐름만 분석하고 Grade Stock의 데이터, 문구, 자산과 디자인 토큰으로 다시 구현합니다.

전역 CSS 파일 하단에 화면별 보정 규칙을 반복해서 추가하지 않습니다. 공통 토큰과 컴포넌트 규칙은 공통 CSS에 두고, 홈·상세·비교 등 화면 고유 규칙은 `body` 범위가 지정된 페이지 전용 CSS로 분리합니다.

기존 HTML 구조를 변경할 때 API 계약과 JavaScript 이벤트 ID, 접근성 속성은 가능한 한 유지합니다. ID 또는 DOM 계약 변경이 필요하면 관련 JavaScript와 리소스 테스트를 같은 기능 단위에서 함께 수정합니다.

상품 레일 헤더는 제목, 짧은 설명, 더보기, 오버플로 메뉴와 좌우 이동 버튼이 하나의 정렬 체계를 사용해야 합니다. 좁은 화면에서는 제목 영역에 제어 영역 폭을 예약해 버튼이 화면 밖으로 밀리지 않도록 합니다.

---

## 디자인 및 기획 규칙

와이어프레임과 신규 화면 구조를 설계할 때 아래 레퍼런스를 우선 확인합니다.

* SaaSFrame (`https://www.saasframe.io/`): 실제 SaaS 페이지, 제품 화면과 사용자 흐름
* SaaSUI Design (`https://www.saasui.design/`): SaaS UI 패턴과 상호작용 사례
* SaaS UI (`https://saas-ui.dev/`): 컴포넌트 구성, 상태, 접근성과 디자인 토큰 구조
* Checklist Design (`https://www.checklist.design/`): 화면, 컴포넌트와 사용자 흐름별 필수 UX 항목
* Design Spells (`https://www.designspells.com/`): 마이크로 인터랙션, 모션과 세부 경험
* UI Bowl (`https://uibowl.io/`): 국내 서비스의 화면, 패턴과 컴포넌트 사례

레퍼런스는 그대로 복제하지 않고 정보 구조, 시각적 위계, 사용 흐름, 컴포넌트 상태와 반응형 전략을 분석해 현재 서비스에 맞게 적용합니다.

한 화면에 여러 레퍼런스의 시각 언어를 무분별하게 혼합하지 않습니다. 화면별 핵심 레퍼런스와 적용 이유를 먼저 정한 후 기존 디자인 토큰과 일관되게 구현합니다.

고객용 커머스 화면은 기존 KREAM·커머스 레퍼런스와 위 자료를 함께 검토하고, 관리자 화면은 SaaSFrame·SaaSUI Design·Checklist Design의 데이터 밀도와 작업 흐름을 우선 참고합니다.

레퍼런스를 확인한 뒤에는 현재 화면과의 차이를 정보 구조, 컨테이너, 타이포그래피, 제어 배치, 반응형과 접근성 기준으로 정리한 다음 구현합니다.

기획 문서는 Manifesto 방식으로 아래 항목을 먼저 정의합니다.

1. 해결할 문제와 작성 목적
2. 대상 사용자와 핵심 시나리오
3. 제품 원칙과 지키지 않을 범위
4. 정보 구조와 사용자 흐름
5. 기능 범위, 상태와 예외 흐름
6. 성공 지표와 완료 조건
7. 구현 우선순위, 의존성과 위험 요소

Claude Design은 초기 디자인 방향 탐색, 대안 비교와 디자인 비평에 활용할 수 있습니다. 최종 화면 구조와 구현 결정은 현재 코드베이스, 접근성, 반응형 검증과 테스트 결과를 기준으로 확정합니다.

와이어프레임을 Figma로 만들 때는 위 기획 문서의 화면 흐름과 상태를 먼저 확정하고, 디자인 토큰과 재사용 컴포넌트를 구성한 후 개별 화면을 제작합니다.

---

## IBSheet 규칙

기존 SaveName 변경 금지

기존 Column Key 변경 금지

기존 RowData 구조 변경 금지

기존 Event 함수명 변경 금지

Header 이벤트 및 기존 시트 동작을 유지합니다.

IBSheet 응답 구조와 서버 응답 필드는 일치해야 합니다.

---

## 코드 리뷰 규칙

Codex Code Review는 이 루트 규칙과 변경 파일 경로에 있는 가장 가까운 `AGENTS.md`를 함께 적용합니다.

리뷰에서는 변경 요약보다 실제 결함을 먼저 보고하고, 심각도가 높은 순서로 정렬합니다.

각 지적에는 아래 정보를 포함합니다.

* 심각도
* 파일과 가능한 한 좁은 라인 범위
* 결함이 발생하는 입력, 상태 또는 실행 경로
* 사용자, 데이터, 보안, 성능 또는 운영에 미치는 실제 영향
* 현재 변경 범위 안에서 적용 가능한 수정 방향

근거가 부족한 추측, 단순한 스타일 선호, 기존 코드에도 동일하게 존재하며 이번 변경으로 악화되지 않은 문제는 결함으로 보고하지 않습니다.

변경 코드뿐 아니라 직접 연결된 호출부, API 계약, DTO, 엔티티 매핑, 마이그레이션, JavaScript DOM 계약과 테스트까지 확인합니다.

다음 항목은 변경 내용에 해당할 때 반드시 검토합니다.

* API URL, 요청·응답 필드, DTO와 DB 매핑의 하위 호환성
* 인증·인가 누락, 개인정보 노출, 입력값 검증과 HTML 삽입 시 escape 처리
* 주문·장바구니·재고·운영 작업의 중복 요청, 경합, 잠금 순서와 트랜잭션 경계
* JPA N+1, 무제한 전체 조회, 페이징 누락과 불필요한 쓰기 트랜잭션
* QueryDSL Projection의 생성자 순서·타입, 동적 조건과 count 쿼리의 일관성
* HTML id·name·class와 JavaScript selector·이벤트의 동기화
* 비동기 요청의 중복 실행, 오래된 응답 덮어쓰기, 실패·재시도·로딩 상태 복구
* CSS와 JavaScript 변경 시 템플릿 자산 버전 갱신
* 데스크톱·모바일의 가로 overflow, 요소 겹침, 잘림과 키보드·스크린리더 접근성
* 신규 테이블·컬럼·인덱스의 운영 마이그레이션과 기존 데이터 호환성
* 정상·예외·경합·회귀 조건을 검증하는 테스트와 변경 범위에 맞는 실행 결과
* 작업과 관계없는 파일, 생성물, 비밀정보 또는 운영 데이터가 diff에 포함됐는지

리뷰 결과는 다음 원칙을 따릅니다.

* 수정이 필요한 findings를 최우선으로 제시합니다.
* findings가 없으면 명확히 선언하고 남은 테스트 공백이나 검증하지 못한 위험만 짧게 기록합니다.
* 동일 원인의 지적을 여러 파일에 반복하지 않고 공통 원인과 영향 범위를 하나로 묶습니다.
* 자동 수정이나 대규모 리팩토링보다 현재 PR에서 해결 가능한 최소 수정안을 우선합니다.

사람 리뷰에서 같은 지적이 반복되면 재현 조건과 필수·금지 패턴을 판정 가능한 문장으로 이 섹션에 추가합니다. 개인 취향이나 일회성 예외는 저장소 규칙으로 승격하지 않습니다.

---

## 테스트 원칙

기능 단위 완료 후 테스트 코드를 함께 보강합니다.

신규 기능은 가능하면 정상 케이스와 예외 케이스를 모두 작성합니다.

신규 기능 개발 시 필요한 테이블, 매핑, 적절한 더미 데이터까지 함께 반영 여부를 확인합니다.

기존 테스트가 존재하면 영향 범위를 검토하고 함께 수정합니다.

프론트 레이아웃을 변경한 경우 데스크톱과 모바일에서 본문 가로 오버플로가 없는지 확인합니다.

상품 레일, 필터, 모달과 고정 행동 영역처럼 밀림이 발생하기 쉬운 요소는 폭이 0이 아닌지, 뷰포트 경계를 벗어나지 않는지 Playwright에서 수치로 검증합니다.

주요 화면 구조 또는 시각적 위계가 변경되면 관련 리소스 테스트와 Playwright 시각 회귀 스냅샷을 함께 갱신하고 데스크톱·모바일 프로젝트를 모두 실행합니다.

장기 캐시되는 CSS와 JavaScript를 변경한 경우 해당 템플릿의 자산 버전도 함께 갱신합니다.

실행 환경 문제로 테스트를 수행하지 못한 경우 명확하게 보고합니다.

현재 환경에서는 Gradle FileLockContentionHandler 권한 문제로 테스트 실행이 실패할 수 있습니다.

실행하지 못한 경우 이유를 반드시 보고합니다.

---

## 보고 원칙

작업 완료 후 반드시 아래 내용을 정리합니다.

1. 무엇을 했는지
2. 이전 대비 무엇이 달라졌는지
3. 운영 영향
4. 성능 영향
5. 유지보수 영향
6. 테스트 수행 여부

필요 시 위험 요소와 추가 검토 사항도 함께 정리합니다.

---

## 안전 규칙

사용자 요청 없이 파일 삭제 금지

사용자 요청 없이 데이터 삭제 금지

사용자 요청 없이 git reset --hard 수행 금지

사용자 요청 없이 운영 설정 수정 금지

사용자 요청 없이 환경 변수 수정 금지

존재하지 않는 테이블, 컬럼, API를 추측하여 생성하지 않습니다.

반드시 코드베이스를 먼저 확인합니다.

---

## 자동화 스레드 정책

1일 1스레드 기준으로 작업합니다.

해당 날짜의 첫 작업은 새로운 대화에서 시작할 수 있습니다.

같은 날짜의 후속 작업은 동일 스레드에서 이어서 처리합니다.

기존 작업 문맥과 결정 사항을 우선 유지합니다.

---

## 응답 원칙

작업 전에는 구현 방향을 간략히 설명합니다.

작업 후에는 변경 파일과 변경 이유를 요약합니다.

분석만 가능한 경우 분석 결과와 다음 작업 계획을 제시합니다.

불확실한 내용은 추측하지 않고 명시적으로 설명합니다.

기존 코드 스타일과 프로젝트 컨벤션을 우선 존중합니다.
